# 企业关账认证

`POST /api/enterprise/close/period-certification` 在财务期间正式关闭前检查子账、银行对账、内部往来、试算平衡、抵销、税务、重大调整、职责分离与管理层签署。

- `CERTIFY`：关账工作与控制证据完整，可以认证期间。
- `RECONCILE`：不存在硬性阻断，但仍需完成对账、抵销、税务或管理层确认。
- `BLOCKED`：内部往来、试算平衡、重大调整、职责分离或审计控制失败。

认证接口可作为 ERP、合并报表和财务报告发布前的统一关账门禁。
